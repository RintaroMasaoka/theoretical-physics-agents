# Phase: Cycle Step 1 — Research Judgment (legacy pointer)

This phase file is kept only so old links do not break. The active `/auto` model no longer has a PI-owned cycle-judgment step. Scientific direction is owned by `research-planner`, and the scheduler only executes the fixed cycle loop described in `SKILL.md`.

Do not execute a separate cycle-judgment procedure from this file. For current direction-setting behavior, follow `.codex/agents/research-planner.md`; for worker/critic launch mechanics, follow `dispatch.md`; for result absorption and graph/fact transactions, follow `.codex/agents/curator.md`.
